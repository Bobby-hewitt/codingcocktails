import React, { Component } from 'react';

import $ from "jquery";

class Helper extends Component {



  render() {
    return (
      <div id="helperContainer">
        <p>"This is all a bit unsightly at the moment but it's your job to fix that!"</p>
      </div>
    );
  }
}

export default Helper;
